[pytest]
pythonpath = src tests
testpaths = tests

markers =
    ephemeral: Local tests (SQLite / TestClient). Safe for CI; data is not kept.
    seed: Hits live Sales (and Catalog) via HTTP; manual only; skipped in CI.
